apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
clientConnection:
  kubeconfig: "{{ deployed_k8s_config_dir }}/kube-scheduler.kubeconfig"
leaderElection:
  leaderElect: true
