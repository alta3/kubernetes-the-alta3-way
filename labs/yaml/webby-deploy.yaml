apiVersion: apps/v1
kind:
metadata:
  name:
  labels:
    app:
spec:
  selector:
    matchLabels:
      app:
  replicas:
  template:
    metadata:
      labels:
        app:
    spec:
      containers:
      - name:
        image: registry.gitlab.com/alta3research/webby 
        ports:
        - containerPort:
